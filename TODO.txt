OK? commands
ΟΚ? stacks
set indexes
OK? checks
OK?    ints
OK?    no dups
ΟΚ? turn into ints
OK? Makefile
sorting


manage errors
	protect mallocs callocs

Makefile
na do ti paizei me libft
	does not run libft rule unless forced (make -B)
	check rm -f before evals




important notes
	segfault stops after adding
		while (iterate->index != minus && iterate->next != NULL)
	->next
	in index.c
	output is still wrong
